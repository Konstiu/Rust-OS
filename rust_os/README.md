# Rust OS

Current Stand, 

`cargo build` build the executable.
From the workspace root, `cargo run -p rust_os_loader` builds a disk image and launches QEMU.





Based on [Writing an OS in Rust](https://os.phil-opp.com/)
